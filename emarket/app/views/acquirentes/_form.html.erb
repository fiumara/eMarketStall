<%= form_with(model: acquirente) do |form| %>
  <% if acquirente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(acquirente.errors.count, "error") %> prohibited this acquirente from being saved:</h2>

      <ul>
        <% acquirente.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
