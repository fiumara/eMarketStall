<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Gestione Account</title>
  <style>
    body {
      font-family: Arial, sans-serif;
    }
    header {
      background-color: #f8f8f8;
      padding: 20px;
      text-align: center;
      border-bottom: 1px solid #ddd;
    }
    nav a {
      text-decoration: none;
      color: #333;
      margin: 0 10px;
    }
    section {
      margin: 20px;
    }
    .account-box {
      border: 1px solid #000;
      padding: 20px;
      margin: 20px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .account-box button {
      display: inline-block;
      margin: 10px;
      padding: 10px;
      background-color: #000;
      color: #fff;
      border: none;
      cursor: pointer;
    }
  </style>
</head>
<body>
  <section>
    <h2>Gestione account</h2>
    <div>
      <form action="<%= gestione_account_path %>" method="get">
        <label for="user_type">Tipo utente</label>
        <select id="user_type" name="user_type">
          <option value="all" <%= 'selected' if params[:user_type] == 'all' %>>Tutti</option>
          <option value="acquirente" <%= 'selected' if params[:user_type] == 'acquirente' %>>Acquirenti</option>
          <option value="venditore" <%= 'selected' if params[:user_type] == 'venditore' %>>Venditori</option>
        </select>
        <input type="text" name="search" placeholder="Cerca" value="<%= params[:search] %>">
        <button type="submit">Cerca</button>
      </form>
    </div>
    <% @acquirente.each do |acquirente| %>
      <% if params[:user_type] == "venditore" && acquirente.negozio.present? %>
        <div class="account-box">
          <div>
            <h3><%= acquirente.nome_utente %></h3>
            <p><%= acquirente.nome %> <%= acquirente.cognome %></p>
            <p>Negozio: <%= acquirente.negozio.nome_negozio %></p>
          </div>
          <div>
          <% if acquirente.bloccato? %>
          <%= button_to "Sblocca", sblocca_acquirente_path(acquirente), method: :patch, class: "btn-unblock" %>
        <% else %>
          <%= button_to "Blocca",  blocca_gestione_account_path(acquirente), method: :patch, class: "btn-block" %>
        <% end %>
        <%= button_to "Elimina", elimina_gestione_account_path(acquirente), method: :delete, class: "btn-delete", data: { confirm: "Sei sicuro di voler eliminare questo utente?" } %>
          </div>
        </div>
      <% elsif params[:user_type] == "acquirente" && acquirente.negozio.blank? %>
        <div class="account-box">
          <div>
            <h3><%= acquirente.nome_utente %></h3>
            <p><%= acquirente.nome %> <%= acquirente.cognome %></p>
          </div>
          <div>
          <% if acquirente.bloccato? %>
          <%= button_to "Sblocca", sblocca_acquirente_path(acquirente), method: :patch, class: "btn-unblock" %>
        <% else %>
          <%= button_to "Blocca",  blocca_gestione_account_path(acquirente), method: :patch, class: "btn-block" %>
        <% end %>
        <%= button_to "Elimina", elimina_gestione_account_path(acquirente), method: :delete, class: "btn-delete", data: { confirm: "Sei sicuro di voler eliminare questo utente?" } %>
          </div>
        </div>
      <% elsif params[:user_type] == "all" %>
        <div class="account-box">
          <div>
            <h3><%= acquirente.nome_utente %></h3>
            <p><%= acquirente.nome %> <%= acquirente.cognome %></p>
            <% if acquirente.negozio.present? %>
              <p>Negozio: <%= acquirente.negozio.nome_negozio %></p>
            <% end %>
          </div>
          <div>
          <% if acquirente.bloccato? %>
          <%= button_to "Sblocca", sblocca_acquirente_path(acquirente), method: :patch, class: "btn-unblock" %>
        <% else %>
          <%= button_to "Blocca",  blocca_gestione_account_path(acquirente), method: :patch, class: "btn-block" %>
        <% end %>
        <%= button_to "Elimina", elimina_gestione_account_path(acquirente), method: :delete, class: "btn-delete", data: { confirm: "Sei sicuro di voler eliminare questo utente?" } %>
          </div>
        </div>
      <% end %>
    <% end %>
  </section>
</body>
</html>
