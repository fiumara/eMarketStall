<%= form_with(model: negozio) do |form| %>
  <% if negozio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(negozio.errors.count, "error") %> prohibited this negozio from being saved:</h2>

      <ul>
        <% negozio.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
