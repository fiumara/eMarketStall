<p id="notice"><%= notice %></p>

<h1>Negozios</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @negozios.each do |negozio| %>
      <tr>
        <td><%= link_to 'Show', negozio %></td>
        <td><%= link_to 'Edit', edit_negozio_path(negozio) %></td>
        <td><%= link_to 'Destroy', negozio, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Negozio', new_negozio_path %>
