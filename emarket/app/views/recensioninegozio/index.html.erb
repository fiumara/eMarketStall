<!DOCTYPE html>
<html>
<head>
  <title>Recensioni</title>
  <style>
    .recensione-container {
      border: 1px solid #000;
      padding: 10px;
      margin: 10px 0;
    }
    .recensione-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .recensione-content {
      margin: 10px 0;
    }
    .recensione-footer {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .recensione-footer button {
      background-color: black;
      color: white;
      padding: 5px 10px;
      border: none;
      cursor: pointer;
    }
  </style>
</head>
<body>
  <h1>Recensioni</h1>

  <div class="recensione-header">
    <h2>Nome prodotto</h2>
    <select>
      <option>Filtra</option>
      <!-- aggiungi opzioni di filtro -->
    </select>
  </div>

  <% @recensioni.each do |recensione| %>
    <div class="recensione-container">
      <div class="recensione-header">
        <h3><%= recensione.prodotto.nome_prodotto %></h3>
        <span>
          <% 5.times do |i| %>
            <% if i < recensione.valutazione %>
              ★
            <% else %>
              ☆
            <% end %>
          <% end %>
        </span>
      </div>
      <div class="recensione-content">
        <p><%= recensione.contenuto %></p>
      </div>
      <div class="recensione-footer">
        <span>Nome acquirente: <%= recensione.acquirente.nome_utente %></span>
        <button>Segnala</button>
      </div>
    </div>
  <% end %>
</body>
</html>
