<!DOCTYPE html>
<html>
<head>
  <title>Gestione Resi</title>
  <style>
    .card-reso {
      border: 1px solid #000;
      padding: 10px;
      margin: 10px;
    }
    .btn {
      padding: 5px 10px;
      text-decoration: none;
      color: #fff;
    }
    .btn-accetta {
      background-color: green;
    }
    .btn-rifiuta {
      background-color: red;
    }
  </style>
</head>
<body>
  <h1>Gestione Resi</h1>
  <% @resi.each do |reso| %>
    <div class="card-reso">
      <h2><%= reso.nome_prodotto %></h2>
      <p><strong>Nome acquirente:</strong> <%= reso.nome_acquirente %></p>
      <p><strong>ID ordine:</strong> <%= reso.id_ordine %></p>
      <p><strong>Data:</strong> <%= reso.data_reso %></p>
      <p><strong>Stato:</strong> <%= reso.stato %></p>
      <p><strong>Motivazione reso:</strong> <%= reso.motivazione_reso %></p>
      <p><strong>Note acquirente:</strong> <%= reso.note_acquirente %></p>
      <%= form_with(model: reso, local: true) do |form| %>
        <%= form.submit "accetta", class: "btn btn-accetta" %>
        <%= form.submit "rifiuta", class: "btn btn-rifiuta" %>
      <% end %>
      <%= form_with(url: reso_path(reso), local: true, method: :patch) do |form| %>
        <%= form.text_area :note_acquirente, placeholder: "Rispondi" %>
        <%= form.submit "rispondi", class: "btn" %>
      <% end %>
    </div>
  <% end %>
</body>
</html>
