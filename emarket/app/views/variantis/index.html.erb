<p id="notice"><%= notice %></p>

<h1>Variantis</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @variantis.each do |varianti| %>
      <tr>
        <td><%= link_to 'Show', varianti %></td>
        <td><%= link_to 'Edit', edit_varianti_path(varianti) %></td>
        <td><%= link_to 'Destroy', varianti, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Varianti', new_varianti_path %>
